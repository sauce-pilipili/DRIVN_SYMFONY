{% for category in categories %}
    <tr>
        <td><a style="cursor:pointer" href="{{ path('categories_edit', {'id': category.id}) }}">{{ category.Name }}</a></td>
        <td class="delete-option-list-index">
           {{ include('categories/_delete_form.html.twig') }}
        </td>
    </tr>
{% else %}
    <tr>
        <td colspan="3">pas de résultat</td>
    </tr>
{% endfor %}